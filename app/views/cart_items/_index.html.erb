<ul id="cart-item-list">
	<% if !cart_items.nil? %>
		<% cart_items.each do |cart_item| %>
			<li id="cart-item-<%= cart_item.id %>"><%= render :partial => 'cart_items/cart_item', :locals => {:cart_item => cart_item} %></li>
		<% end %>
	<% end %>
</ul>