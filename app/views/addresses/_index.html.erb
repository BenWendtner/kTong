<% if !addresses.nil? %>
	<% addresses.each do |address| %>
		<li id="address-<%= address.id %>" class="address-list-item">
			<div>
				<%= render :partial => 'addresses/address', :locals => {:address => address} %>
			</div>
		</li>
	<% end %>
<% end %>