$('#address-<%= @address.id %>').remove();

var selected = false;
$('.address-list-item').each(function() {
	if ($(this).hasClass('selected'))
		selected = true;
});
if (!selected)
	$('#link-to-checkout-payment').hide();
